<!DOCTYPE html>
<html>
  <head>
    <title>{{conf.title}}</title>
    <meta name="description" content="{{conf.description}}">

    <!-- Mobile Meta -->
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1">

    <!-- IE Meta -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">

    <!-- Load modernizer first for features detection. -->
    <script src="/vendor/modernizr.js"></script>

    <!-- Styles -->
    {{#each conf.styles}}
      <link rel="stylesheet" href="{{path}}" type="text/css">
    {{/each}}

  </head>

  <body>

    <div class="slide-panel">
    </div>

    <header class="text-centered animated">
      <a href="/"><div class="brand"><img src="{{{conf.brand}}}" /></div></a>
      <h1 class="hidden">{{{conf.title}}}</h1>
    </header>

    <nav class="nav header text-centered">
      {{#each conf.pages}}
         <a href="{{{this.path}}}">{{{this.title}}}</a>
      {{/each}}
      <a href="/search"><span class="ion ion-search"></span></a>
    </nav>

    <main>
